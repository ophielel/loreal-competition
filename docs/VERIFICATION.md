# 交付验证记录

日期：2026-09-06。环境：Windows、本机 Python、Microsoft Edge / Playwright。

## 自动化验证

- `python -m unittest discover -s tests -v`：20 项通过。
- `node scripts/browser_test.cjs --record`：19 项真实浏览器检查通过，控制台错误 0。
- 覆盖：时间截断、归档隔离、标签不进入推断、健康风险不被后续消息或模型降级、未知证据拒绝、缺钥/非法输出回退、模型适配器成功与缓存路径、SQLite 幂等/持久化、API 参数和跨站请求拒绝。
- 浏览器覆盖：138 会话、搜索/风险筛选、回放、归档工单、证据定位、建议编辑和模拟发送、任务确认与完成、评估表、导出、空结果、键盘焦点、320/390/768/1024/1440 像素无页面横向溢出。
- 模型适配器测试使用本地构造 HTTP 响应，不是实际在线 Qwen 调用。

## 文件验证

- PPT 为 13 页，保留官方模板背景和品牌装饰；已由本机 PowerPoint 打开并导出 PDF 和逐页 PNG，查看联系表检查布局。
- 视频来自实际浏览器操作，约 80 秒，H.264 MP4；中文字幕压入画面，无配音。
- 源码 ZIP 使用文件白名单，排除 .git、真实环境文件、密钥、依赖缓存和本地任务数据库；附数据、原始材料、脚本和运行指南。
- 原始工作簿 SHA-256：b5ac027e863c5580dab39c8f459e4698d65e9fbec29832c9915448f2087307b7。

## 实际限制

未使用真实 API Key 在线测试 Qwen；未接入真实千牛/物流/支付；未做独立盲测、真实客服实验或生产负载测试；未上传天池。队伍名称为用户指定的“待填写”。
